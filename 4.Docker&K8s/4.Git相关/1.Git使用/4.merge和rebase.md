我们在做分支合并时，有两个常见的操作：

git merge 和 git rebase

它们的关联和区别在哪？

![image-20231214153843223](image/image-20231214153843223.png)

其实我们看这两个所用介词的不同，就知道了，一个是 onto，一个是 into

简而言之，git merge 将两个分支的最新更改合并为一个新的提交，形成一个新的合并节点；而 git rebase 是将另一个分支的提交记录移动到当前分支的提交之前，整理提交历史，使提交记录更加线性清晰。

