`Redis`事务的语义边界与`MySQL`不同，它保证同一批次命令按顺序执行，并且执行期间不会被其他客户端插入命令，但它不支持原子回滚，即如果某条命令执行失败，不会影响后续执行，之前已执行的命令也不会回滚。

这是因为`Redis`的内部没有`undo log`，没有版本链，也没有事务隔离级别。

`Redis`事务只涉及四个核心命令：`MULTI`、`EXEC`、`DISCARD`、`WATCH`。

待办