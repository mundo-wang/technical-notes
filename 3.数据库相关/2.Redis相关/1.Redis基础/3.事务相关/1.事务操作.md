`Redis`事务的语义边界与`MySQL`不同，它保证同一批次命令按顺序执行，并且执行期间不会被其他客户端插入命令，但它不支持原子回滚，即如果某条命令执行失败，不会影响后续命令执行，之前已执行的命令也不会回滚。

这是因为`MySQL`依赖`Undo Log`机制和`MVCC`版本控制来支持事务的隔离和回滚，而`Redis`作为内存数据库，追求高性能和简单模型，不维护事务回滚信息，也不提供事务隔离级别，因此，从机制上讲，`Redis`不支持对已执行的操作进行回退。

`Redis`事务涉及四个核心命令：`MULTI`、`EXEC`、`DISCARD`、`WATCH`。

待办